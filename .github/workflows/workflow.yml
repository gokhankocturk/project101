name: Deploy CloudFormation

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Install AWS CLI
      run: |
        npm install -g aws-cli

    - name: GOKHAN
      run: |
        echo "gokhan kocturkkk"
        
    - name: Deploy CloudFormation
      run: |
        aws configure set aws_access_key_id AKIAVUSSDOVCE3PO4PVM
        aws configure set aws_secret_access_key 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
        aws configure set region us-east-1
        aws cloudformation deploy --template-file deneme.yaml --stack-name yaml-latestami
