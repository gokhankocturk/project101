name: Deploy CloudFormation

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Install AWS CLI
      run: |
        npm install -g aws-cli

    - name: GOKHAN
      run: |
        echo "gokhan kocturk"
        
    - name: Deploy CloudFormation
      run: |
        aws cloudformation deploy --template-file deneme.yml --stack-name yaml-latestami --capabilities CAPABILITY_IAM
